#ifndef UTIL_H
#define UTIL_H
#include <stdio.h>
#include <stddef.h>
// char pegaID(char[255]);
void obterTresPrimeiros(char* str, char* destino, size_t tamanhoDestino);
void removerNovaLinha(char* str);

#endif